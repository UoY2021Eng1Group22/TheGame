package uoy.thegame.entitymodel;

// Here lies the lane separator (used for collision detection)

import com.badlogic.gdx.graphics.Texture;

public class LaneSeparator extends Entity {
    public LaneSeparator(float x, float y, Texture texture) {
        super(x, y, texture);
    }
}
